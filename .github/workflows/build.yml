        
name: Build TWA AAB
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install Bubblewrap
        run: npm install -g @bubblewrap/cli
      - name: Init TWA
        run: echo "n" | bubblewrap init --manifest https://graceful-tulumba-ff85c3.netlify.app/manifest.json --directory twa-app
      - name: Build AAB
        run: |
          cd twa-app
          bubblewrap build
      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: infinity-heals-aab
          path: twa-app/app-release-bundle.aab
